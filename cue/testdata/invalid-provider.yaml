# INVALID: providers entries must declare one of the supported types
# (a wire protocol or a known service). "telegram" is neither.
# Note: model.provider itself may name any configured providers key, so
# unknown active providers are rejected by config.Load's resolution step
# rather than by this static schema.
model:
  provider: openai
  name: gpt-4o-mini

providers:
  weird:
    type: telegram
