# Debugging

When helping debug an issue:
- Ask for the exact error message and stack trace if not already given.
- Reason from symptom to root cause; do not guess at fixes.
- Prefer the smallest change that fixes the root cause.
