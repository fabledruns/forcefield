# Forcefield RC5 — Commit 100 Forensic Audit (AUDIT ONLY, no fixes)

**Target:** `136c0b4` (`git rev-list --count HEAD` = 100) + dirty working tree as found on disk.
**Date:** 2026-09-07. **Mode:** hostile, read-only (this report file is the only write, per explicit user request).
**Tooling run:** `go vet ./...` clean, `go build ./...` succeeds, `go test ./...` all ok, `go test -race ./internal/runtime/ ./internal/session/` passes.
**Baseline problem:** audited tree != HEAD. `git status --short`: `D audit/AUDIT_* (2)`, `M (13: go.mod, command/*, runtime/*, shell/jobs.go, tui/*)`, `?? (7: audit/5DAY_*, command/builtin/new.go, runtime/loop_detector.go, tools/run_control.go, tui/loading.go+test)`. `git diff --stat`: 514 insertions. The P0-P1.14 hardening (`dc4d2e1`, 102 files, +11309/-571) IS committed; the loop-detector / RunControl / EventCancelled / runMu / sessionEntries-tool-render / loading wave is UNCOMMITTED. Claims about "Commit 100" are irreproducible from HEAD alone. Finding FF-100-DIRTY.

Previous audit: `audit/5DAY_AUDIT_REPORT.md` (830 lines, target `e10e62a`+dirty, score 40/100, NOT YET).

> Do not trust docs. Every section cites `file:line` verified on disk. Severity: CRITICAL/HIGH/MEDIUM/LOW/INFO. Confidence: CONFIRMED/HIGHLY LIKELY/PROBABLE/POSSIBLE. Category labels per finding. No fixes applied.

## 1. Architecture

Map: `main.go:25` -> `cmd.Execute()` -> `cmd/{root,chat,run,doctor,memory}.go` (Cobra, only 4 commands — no init/tools/sessions). `internal/`: 15 packages, ~300 .go files, ~51k lines incl. tests (~25k non-test). Largest: `runtime/runtime.go:2145`, `tui/model.go:1594`, `tools/shell/shell.go:937`, `providers/openai_compatible.go:631/responses.go:615/anthropic.go:597/gemini.go:546`.

Boundaries: runtime owns multi-turn loop; providers stream ONE turn (`providers/provider.go:12-13`, `runtime/runtime.go:1771`). Providers never execute tools; tools never manage sessions. TUI presentation-only (`tui/model.go:92-95`) except turn-repair duplication (FF-100-025). No import cycles. Hub `runtime/runtime.go:15-27` imports 13 internal pkgs — intentional god-object per AGENTS.md.

State: Session `session/session.go:27-43` mutated only on Bubble Tea goroutine — but TUI-owned, runtime never touches TurnState (grep BeginTurn/EndTurn hits only `tui/model.go`, zero in `runtime/`). Headless `ff run` (`cmd/run.go` imports no session) has no TurnState/heal. Design weakness. Runtime `runMu:65` (dirty) serializes loops, `mu:73` guards switchable cfg/provider/agent, snapshot `runSnapshot:1552` via `snapshotRunState:1572`. SetAgent atomic rebuild+rollback; SetModel/SetProvider in-mem until SaveConfig. Task `task/state.go:84-89` per-run, FinalStatus never upgrades without recorded passed. Permissions `permissions/manager.go:12-21` (mu+saveMu), session overlay `scheduler.go:63-64` explicitly not persisted. Config APIKey `yaml:"-"` never on disk, secrets via env/.env + `redact.AddSecret:401-403`. Correct.

Goroutines/channels/locks: providers each `go func() events chan`; `runtime.go:1632` one producer + runMu; `scheduler.go:158-159 sem(4)+wg+stopped`, `askMu:56` (new), `emitMu:151`, `managerMu:47` overloaded for manager+asker; `shell.go:487,493-494,497` pipes; `jobs.go` same; `tui/discovery.go:44` detached Background fetch (leak).

Global state: only true mutable global `redact.secrets+secretsMu:70-73` (capped 64 FIFO). Rest immutable or guarded. Good. Filtered shares Tool instances; projectMemoryText cached at construction (stale until restart); workspaceRoot/Cwd fixed; StdinAsker parks one goroutine per prompt; events cap 1 + blocking sends = backpressure by design. Single points of failure: single Messages slice (1000 cap, in-mem unbounded within run), single model struct (1594 lines), single session file per session (last-writer-wins, no lock).

## 2. Agent loop / runtime

Lifecycle `runtime/runtime.go:1606 StreamChat ->1631 chan(1) ->1637 runMu ->1639 run:1771 ->1793 loop ->1798 BeginIteration ->1799 MaxIterations ->1804 refreshSystemPrompt ->1810 windowForProvider ->1813 runModelTurn:2158 ->2198 streamOneTurn (select ctx.Done vs stream:2215) ->1827 ToolCalls? ->1831 FinishLength->Blocked ->1841 MaxToolCalls ->1846 append assistant ->1852 guard ->1856 freshCallsForLoopDetection ->1857 executeCalls:1929 (dup-ID reuse:1936, maxConc:1955) ->1866 RecordTool ->1867 truncate6000 ->1868 Scrub ->1869 Fence ->1870 append ToolRole ->1882 Observe ->1887 MaxConsecutiveFailures -> Done/Blocked/Error/Cancelled`.

SECURE: DefaultLimits{60,300,5}:47-51 + window + FinishLength->Blocked + loop-detector(limit 3) guarantee termination by default. Dup-ID replay reuses recorded result, never re-executes (1936-1944, copy so no note accumulation). askMu+emitMu+caps-gated concurrency correct. Turn states monotonic (session.go:371-424); CancelTurn idempotent; Repair synthesizes results. Cancel distinct from error (event.go:26 EventCancelled, dirty).

DIRTY CAVEAT: loop_detector.go (untracked), run_control.go (untracked), runMu/EventCancelled/cancelled() (+108), jobs.go reaped+RunControl (+36) uncommitted. Committed HEAD has NO loop detector, NO EventCancelled, NO runMu.

Weaknesses: nondeterministic output resets detector (loop_detector.go:70-72); empty-ID repeats execute 3x before stop (1935 only dedupes ID!=""); abandoned undrained+uncancelled events chan leaks run goroutine holding runMu, deadlocking next StreamChat (1631,1652-1660); 10s coalescing (session.go:471) splits batch if Ask>10s; TurnState TUI-only.

## 3. Long-horizon

Per-turn provider view is TRUE sliding window (maxContextMessages=100:57, SelectContext:context.go:166 pair-aware, token+count). Session file bounded (maxSessionMessages=1000:124). Shell 2MiB, timeout 300s, 5xx/timeout retried, asks serialized, tool history rendered after resume (dirty model.go:381-402).

Still unbounded: in-mem messages every iteration (assistant+N tool msgs ~6k), never compacted; only request view windowed. Bounded by 60 iters by default, but Limits<=0=no limit (runtime.go:32-33) grows messages/executed/loop maps unboundedly. task.State Discoveries/Blockers/Plan append forever (state.go:172-189); Summary caps discoveries at 8 but emits ALL blockers+plan steps, inflating pinned system prompt. Session 1000 cap is silent drop (no marker), no rotation/GC/delete, no TTL. List() reads all *.json every picker open — O(n). Traces per run, crash files never cleaned (trace.go:127-130), one FD per concurrent run. Memory entries unbounded. write_file input unbounded (disk fill). TUI transcript O(n) rebuild per event. No log/compaction/auto-restart/SIGHUP; ff run ephemeral.

Failure budget: 429 transient yes; quota no (correct); 500/502/503/timeout pre-200 YES (fixed); mid-stream NO (by design); tool crash yes; hang yes (Unix pgroup, Windows best-effort); Forcefield crash: last completed tool saved, 1 iter lost, no recover/log; terminal close: no SIGHUP need tmux; reboot: manual --resume, cwd-relative trap; disk full: diverges; huge output: capped (fixed); context 400: windowed (fixed) except system bloat; concurrent ask: fixed; shell /mnt: no.

## 4. Context management

buildMessagesWithBudget:1521 (fresh system + refreshSystemPrompt:2048 digest) + budget.SelectContext. History already truncate->Scrub->Fence (1867-1869, fence <tool_result tool=%q> scrub.go:12-14). windowForProvider:1538 every turn. maxToolResultChars=6000:1768 rune-boundary+marker. ContextBudget{Limit,Reserve,MaxMessages,Summarize}:58-68; BudgetForCaps override->live->static->defaults; unknown->count-only. DefaultReserve 4096.

Good: count+token dual caps, atomic assistant-tool_calls groups (330-361), optional digest (296-302), FinishLength->Blocked:1831.

Broken/weak: default Summarize:false:77 -> silent drop. System ALWAYS emitted full (SelectContext:271) even when systemTokens exceeds tokenRoom (room negative:215, oversized system still sent). task Summary:279-289 unbounded Blockers+full Plan; task_tool.go:178-204 no max steps. Model can bloat every future request; windowing cannot shed it. EstimateTokens rune/4 undercounts CJK/emoji ~4x. compactIfNeeded persistence-only; ProviderMessages:158-176 replays up to 1000 (runtime windows per-turn, wasteful).

## 5. Providers

All use defaultRetryPolicy{3,1s,30s,60s}:41-46 via doWithRetry:171. ResponseHeaderTimeout:120s, Client.Timeout:0 (http.go:15-23) — body unbounded, headers bounded.

- 429 transient: RESOLVED (kept) retry.go:236-261,131-144.
- 429 quota: RESOLVED (kept) quotaPhrases:107-114.
- 5xx !=501: RESOLVED (was no retry) isRetryableServerError:150-161,264-281.
- Transport timeout/connection pre-200: RESOLVED (was no retry) 189-214.
- 120s wall kills streams: RESOLVED for body (Timeout==0).
- Slow TTFB>120s (Ollama cold): STILL PRESENT (Kimi-only 300s openai_compatible.go:75-81,nvidia.go:19) ollama.go:132.
- Mid-stream resume: STILL PRESENT by design (163-168 replay would duplicate).
- 401/403/400/404/402/501/quota/cancel/protocol never retried: Correct.
- Secrets redacted, key off URL, discovery hashed: Mostly RESOLVED (residual raw statusError.Body via errors.As, unredacted mid-stream chunk.error).

Per-provider bugs (CONFIRMED): Ollama never emits StopReason/Usage bare Done (ollama.go:315-317); Gemini tool turns FinishStop not FinishToolCalls (193-201,428-430), synthetic call-%d atomic (23-28), requests thinkingBudget (334-353) but never emits Thinking (all ->Text:443-447); Ollama clean EOF->UnexpectedEOF (274-280); Ollama ListModels reuses *http.Request (84) violating single-use contract; sse.go:54-56 oversize as Unknown not Protocol. context_limits.go is ONLY window tables — old attribution wrong; fix lives in http.go+retry.go+transient.go.

## 6. Tools

Canonical bounds tools/limits.go:15-44: shell 2MiB/30s/300s ceiling, read 5MiB, list 500, search 100, find 50, git 256KiB, job 1MiB/300s, secret 50, generic 30s. ClampTimeout, TruncateString, CapLines. Config tools.*.timeout 0..300.

read_file 5MiB + O_NOFOLLOW confined (Windows no-op); write_file EnsureWithinWorkspace+MkdirAll+re-validate+EvalLinks+Lstat+O_NOFOLLOW (Unix) but NO cap on content input (disk fill); list 500+meta; pwd Getwd no policy (intended); search/find excluded dirs, 512KiB/file skip, 2000 files/line caps, EvalLinks both modes, IsSensitivePath skip — but local within() case-sensitive (search.go:424-437) vs sandbox case-folding Windows (bypass aid); git closed allowlist, fixed argv+-- scope, runCapped, dead unbounded runGit retained; secret_scan one-of path/text, 1MiB, 50 findings, 2+2 redaction — but no O_NOFOLLOW/re-check, Stat->Read TOCTOU (narrow; scheduler forces Ask).

Pipeline Model JSON->finalizer->Lookup->checkPermission->MetadataOf->execute->StringArg->sandbox->os.*. Validation AFTER permission (scheduler.go:239-253 after :255): extra ignored, wrong-type defaults. Gap retained.

Permissions: session-scoped sessionAllow map[string]Decision (scheduler.go:58-64,409-423,537-542) — improvement (no longer config.yaml), but still per-tool-name: one Always allow shell authorizes ANY future command in session. Check=Tools[name] else Default. Sensitive escalation isSensitiveCall:390-407 forces Ask even when Allow for read/write/list/search/find/git/secret_scan:path + shell_job:cwd — but shell:command ABSENT; shell_job checks cwd not command. Lexical pre-resolution; symlink/rename bypasses gate.

Fencing/scrub: FenceToolResult (scrub.go:12-14) truncate->Scrub->Fence (runtime.go:1867-1869); live chunks scrubbed unfenced. NO escaping of </tool_result> — injected close breaks fence. Session.AddProviderMessage:187-195 scrubs content+args, AddPendingCall scrubs copy, scheduler scrubs content/stdout/stderr+chunks — mostly resolved, one CONFIRMED narrow vuln: redact.ScrubMap shallow (top-level strings only, nested map passthrough redact.go:148-161). shell.env:{KEY:secret} persists raw in tool_calls.arguments.env + pending.

## 7. Shell

command required non-empty (296-302), interactive refuse (312), WSL lexical (329), cwd via Prepare (392-421), timeout_seconds >300->ArgumentError (355-365)+ClampTimeout. FF-SEC-011 RESOLVED. Stdin=nil never terminal, setProcessGroup+Cancel=killProcessGroup+WaitDelay=1s:439-441, explicit os.Pipe (448-459), Start:462, concurrent streamPipe+Wait, bounded drain. Output RESOLVED shellOutput{max:MaxBytes} (484, 2MiB) + meta+marker. Timeout/cancel runCtx WithTimeout:388 + scheduler outer double-bounds; Unix pgid+SIGKILL; Windows setProcessGroup no-op, taskkill /T /F +Kill fallback — grandchildren without job object survive; WSL relay no pgid best-effort. STILL PRESENT platform gap. Interactive -c recursion added (869-882) — prior bash -c bypass RESOLVED; still heuristic (841-848 deliberately simple): find -exec vim, python -c, git commit editor, heredoc/$()/quoting not parsed. Real containment Stdin=nil+timeout/kill. Env full host forwarded natively by design; extraEnvArgs does NOT validate k (shell.go:987-994), only WSL spill validates. shell_job start does NOT call isWSLForbiddenPattern (only shell.go:329) — parity gap.

## 8. Filesystem / sandboxing

ResolveWithinWorkspace (policy.go:55-138): anchored to workspace, Abs+Clean, EvalLinks (248-309), withinAny, Windows case-insensitive within:314-327, abs-like rejects. EnsureWithinWorkspace:160-234. Policy.Confines()=wsl||Strict (sandbox.go:115-118). builtin uses WithPolicy only if Confines(), else unrestricted (builtin.go:96-161).

Default native+permissive -> ZERO confinement (config.go:259,271, sandbox.go:122-124). Any absolute path readable/writable/searchable. read/list/search/find/git/secret_scan:allow by default (236-242). STILL PRESENT by design, now honestly labeled. Fresh install reads .env/.ssh zero clicks -> provider -> session replay.

WSL shell escapes: wsl_windows.go:38-43 honestly NOT confined /mnt/*. isWSLForbiddenPattern lexical /mnt/,C:,wsl,/proc/,/sys/,../,powershell (shell.go:727-760) bypassable via $a=/mnt, quoting, /proc/self/root. Comment 324-328 mitigation not boundary. STILL PRESENT documented non-boundary.

native Strict overclaim: nativeExecutor.Describe Strict returns FilesystemConfined:true (native.go:72-84) contradicting sandbox.go:242-245 For shell NO BACKEND SETS TRUE. Strict cages FS tools+cwd, shell text (cat /etc/passwd) unconfined. Approval UI from Describe (scheduler.go:525-530) overstates. CONFIRMED weakness.

Dotfiles: no block; IsSensitivePath covers .env*,*.pem,.ssh/,id_*,.aws/.azure/.gcloud/.kube/.docker (sensitive.go:12-63) — not .git/, generic dotfiles, *.sqlite. Lexical only. TOCTOU: re-validate+O_NOFOLLOW Unix; Windows plain Open/WriteFile no-ops; secret_scan neither. Scheduler concurrency 4 widens window.

Secure: ResolveWithinWorkspace+EvalLinks+withinAny strict/WSL; validID; 0600/0700+atomic; secrets never in config.yaml/os.Environ WSL; x-goog-api-key header; Redacted errors.

## 9. Security regression

FF-SEC-001 WSL /mnt: STILL PRESENT. FF-SEC-002 native auto-allow: STILL PRESENT (defaults unchanged). FF-SEC-003 Always: PARTIALLY (session-scoped, still per-name). FF-SEC-005 OOM: RESOLVED (2MiB). FF-SEC-006 injection: PARTIALLY (fence no escape, memory unfenced). FF-SEC-007 session leak: PARTIALLY (scrub boundaries, nested-env leak, plaintext 0600). FF-SEC-009 concurrent ask: RESOLVED (askMu). FF-SEC-004 bash -c: PARTIALLY (recursed, heuristic bypassable). Untrusted repo -> read_file -> ToolRole fenced-unescaped -> hijack -> shell -> exfil. update_task_state passed trusted without evidence. No deser exec. Session JSON bounded by 1000.

## 10. Sessions

Creation manager.go:24-33; switching model.go:1263-1318 (Load+heal+stopStream+SetAgent+swap+refresh, gen drops stale); deletion MISSING (no Delete/GC/TTL, dir grows); loading/corruption CORRECT (validID:40-52, Load:183-213 never deletes, ListCorrupt:242-280 isolates, doctor surfaces). Rotation NONE; compaction YES silent (maxSessionMessages=1000:124, keep [0] if user + newest 999, no marker, from AddProviderMessage:205/AddMessage:175/Save:80). Provider window separate (100). ProviderMessages:158-176 replays up to 1000 (runtime windows per-turn). AppendToolCallToLastAssistant:462-488 requires assistant+ToolCalls,<10s,ID-dedup — heuristically fixed; residual time not Turn.ID, resume<10s coalesces wrong, second text dropped. UpdatedAt churn NOT fixed (Save:81 unconditional + every mutation; list sort churns).

## 11. Memory

Entry{ID,CreatedAt,Text}; Load oldest-first; Add TrimSpace, Scrub:155, dedup exact EqualFold only, 8-char ID; FormatForPrompt renders ALL, no cap/TTL/ranking. runtime.go:153-161 loads CurrentProjectStore once, caches projectMemoryText:109 — add_project_memory writes file but prompt stale until restart. project.go git rev-parse else cwd, slug-hash12. GlobalStore NEVER loaded into prompt (dead storage). No maxEntries/maxBytes. Poisoning: memory unfenced (agent.go:61-72) vs tools fenced; add_project_memory Requires approval only via permissions ask template — if allowed, ignore previous instructions becomes system instruction. Secrets scrubbed before persist (correct).

## 12. Persistence

CORRECT: tmp-same-dir + Write+Sync+Close+Chmod + Rename retry x5 (Windows AV) for sessions (manager.go:62-137,152-163), memory, config; 0600/0700 preserve; failure restores in-mem; corrupt never rewritten; List ignores non-.json (tmp accumulates, no GC); validID; Turn Recover(interrupted,never re-run)+Repair(synthesized cancelled,idempotent,paired)+heal. GAPS: no locking (last-writer-wins, durability_test 8 concurrent -> final 1 asserts parseability only). Session/Memory/Config no mutex (TUI single-thread only). No dir fsync (power loss loses rename). TUI batch+pending one Save, result+resolve one Save (atomic) — but session order follows completion (emitMu), runtime mem follows call order — replay order differs across restarts (ID pairing valid). ff run ephemeral. Killed at exact instruction: before first Save lost (Turn nil, heal no-op, user msg saved safe); after batch Save before exec -> interrupted+synthesized cancelled never re-executes (correct); during/after exec before result Save -> real result lost marked interrupted (documented loss); during temp -> old intact + orphan tmp; during Rename atomic old-or-new; EndTurn(mem)-Save gap mislabels complete as interrupted (harmless).

## 13. Concurrency

go test -race passes. Guarded: runMu/mu/reasoningMu, scheduler managerMu/askMu/sessionMu/emitMu, sem+WG+stopped, pre-start ctx.Done->cancelledResult eventless, in-flight exactly-1 Cancelled, permissions mu/saveMu, discovery, shellOutput mu, RunControl WG+Once, secretsMu, explicit pipes+Wait+waitDelay+bufio.Reader+sanitize. Leaks/stalls: Session/Memory/Config no mutex; stdin.go:68-84 parks goroutine per prompt + new Reader per Ask over-read risk; tui/discovery.go:44-49 Background fetch per picker open until 20s (single-flight waiter cancels, fetch continues); emitMu held while events<- blocks stalled consumer (by-design backpressure stalls others).

## 14. Cancellation

Can CURRENT op stop without killing app? YES. model.go:432-435 Ctrl+C preempts even modal; runActive:713-715; cancelActiveRun:721-731 (cancelStream+gen+++clear+save partial+CancelTurn+Repair+Save); gen drops stale; runMu serializes; cancel buffered 1 + non-blocking if detached (runtime.go:1631,1639-1650) else backpressure with ctx.Done. Chain: model pre-check + backoff select + streamOneTurn select ctx.Done vs stream (hung provider ignoring ctx still terminates, run_cancel_test); runtime ctx.Err->cancelled(cleanup.Wait()+emitCancelled); scheduler callCtx WithTimeout:285 parent-cancel->ToolCancelled stops retries, Ask cancel->Cancelled not Denied; shell runCtx Timeout:388 Prepare(runCtx) Cancel=killProcessGroup (Unix pgid+SIGKILL; Windows taskkill best-effort; WSL no pgid); persistence per-terminal Save; UI maps Cancelled/Blocked/Error/Done; cmd/run.go:51-52 NotifyContext -> RunContext -> mapRunError preserves Is(Canceled). Intentional drops: EventCancelled default-dropped if retired (already tore down); pre-start cancel eventless (recorded terminal for idempotency); stdin cancel ->DenyOnce+ctx.Err remapped to Cancelled (correct).

## 15. CLI/TUI/UX

4 Cobra commands only (root/chat/run/doctor/memory) — no init/tools/sessions/resume despite README.md:237 ff init and /resume. /help live, Levenshtein <=3, prefix+Tab+click, /status/tools/agent/skills, picker local/cloud+caps+key-missing no I/O, lazy discovery+Fetching+refresh, permission Tool:name+sorted key:value+300B+scrub+honest no-isolation note. Header idle/thinking/running/waiting, 8-block loading wave, per-ID live tool lines + progress + finish icons/counts/duration + expand Ctrl+E/click 40 lines, thinking collapsible never in session, following stick+wheel 3, resize width-reparse/height-avoid, measured header, pickers clip 5-15, paste bracketed+25ms+CRLF->LF, CharLimit 20000, Alt+Enter/Ctrl+J newline, input 1-6 rows, markdown stable/tail + per-width glamour cache + ASCII guard, OSC52 copy, splash empty, Init blink, !ready starting line.

Fixed dirty: sessionEntries:358-409 renders tool roles as finished collapsed blocks (prior FF-TUI-001 dropped them). loading.go new.

Gaps: footer omits Tab/Ctrl+J/Ctrl+Y/Ctrl+C/F2/click; exact-match hides suggestions; Tab only command name; submit-while-busy drops Enter silently (1054-1060); copy silent OSC52 to Stdout while alt-screen risk; ff chat --resume X trap (chat.go always newChatSession, --resume only root); /sessions swallows IO as No saved sessions; doctor os.Exit(1) in RunE; cancel vs error styling inconsistent (roleSystem Run cancelled vs roleError context canceled); /status ~X of context divides bytes; transcript provider errors uncapped; permission-cancel no verdict line; no picker search/delete/preview; sessions cwd-relative (./.forcefield/sessions) so --resume elsewhere -> no saved session (undocumented); first-run Printf Created default config pollutes ff run stdout (breaks scripts); Version dev unless ldflags; no ff update.

## 16. Ease of use

Installers ~/.local/bin, PATH note+restart, no admin, checksums HTTPS-only honestly scoped. No init wizard (implicit Load; must discover doctor). Defaults ollama/localhost:11434/ornith:9b — if down first prompt generic stream failed (model.go:1077) no pointer to /provider/doctor (doctor explains). Pickers lazy never at startup (fast, bad first-run). Recovery best-in-class, cancel visible, sessions cwd-trap. Update rerun installer, removal leaves config/sessions (predictable). No flags/env for provider/model (only --agent), CI ff run cannot override model without YAML. agents.<name>.provider/model in-mem only silent on failure.

## 17. Cross-platform

Home os.UserHomeDir (tests HOME+USERPROFILE); sessions Join+FromSlash; commandBaseName splits /+\ explicitly; 0600/0700+rename retry AV; seamed exes (bashLookPath,wsl.exe LookPath,gitBinary); CRLF handled; WSL blocks honestly mitigation. Findings: native Unix hard-requires Bash (native_unix.go:29-35,41-44) minimal/Alpine fails; git for workspace/memory root (project.go:38 fallback cwd silently, doctor no git check); OSC52+mouse-cell-motion always on (tui.go:33-37 Shift+drag needed, F2 undocumented, some terminals ignore silently); no line-ending normalization session JSON (cosmetic). Makefile OS check MinGW; clean rm -rf breaks PowerShell.

## 18. Config

Shape documented, CUE mirror. Secrets env > ./.env > ~/.forcefield/.env (370-407), never into os.Environ. No api_key YAML by design, Save never round-trips APIKey, 0600/0700+repair. Fail-fast field-named errors. Unknown agent/tools rejected; unknown skills warn runtime (correct). Compat legacy endpoint, agent default->general, Session.Agent omitempty->general, Turn nil. Insecure-by-choice honestly labeled native NO isolation, permissive historical; doctor warns NOT. MUST-FIX: docs/Config.md:265 claims /model /provider SetModel/SetProvider in-memory only do not write config.yaml. Code persists: tui/commands.go:130-155 SaveConfig() returns save errors. One wrong; restart unpredictable. No flags/env provider/model (only --agent); agents.<name>.provider/model in-mem silent leaves old.

## 19. Errors

Scheduler IsError deterministic never retried vs err infra retried iff Metadata.Retryable (309-343), shell Retryable:false correct; backoff cancellable; attempt surfaced; dedupes IDs; loop/length -> Blocked actionable; FinishLength->Blocked good (except with calls). EndTurn only in_progress->terminal; Resolve ignores unknown/double-terminal; CancelTurn idempotent. Thin: ff run raw err, TUI stream failed: %v no hint to doctor/key; corrupt config path good but creation notice stdout mixes stderr for scripts; root.go:51 _ = sess.Save() ignores disk-full on agent switch; doctor.go:355-357 swallows override errors.

## 20. Observability

No telemetry, opt-in disabled default JSONL per-run, 4MB cap+truncation, 1024c snippets, Scrub/ScrubMap every free field, nil-safe no-ops, skips deltas keeps turn/tool boundaries, crash files kept. Sessions scrubbed before persist, pending ScrubMap, errors ScrubError preserving Unwrap. Central patterns+exact env, FIFO 64, min 8c. Doctor scrubs every line, key presence-only, per-type probes, 3s timeout. Tool results stdout/stderr/exit/duration/attempt; expanded view all; permission scrubs before truncate. Too little: no --verbose/log/token counts (only ~chars); trace off default no indicator; reproducing retries requires code; trace dir per-cwd scatter no doctor line; ScrubMap overpromises (non-strings pass); transcript error uncapped.

## 21. Resources

Per-turn bounded: context window (100+tokens+reserve), tool caps (2MiB shell,5MiB read,500 list,100 search,50 find,256KiB git,1MiB job,50 findings)+6000c+Truncate/CapLines, SSE 1MiB, error 8KiB, non-stream 64MiB, discovery 10m single-flight, scheduler 4, DefaultLimits 60/300/5, MaxTimeout 300s, jobs 4/32/TTL 10m+deadline. Good. Unbounded: in-mem messages within run; session file count (no GC)+List O(n); trace file-count/FD per concurrent run; memory entries; write_file input (disk fill); TUI transcript mem+O(n) rebuild; single turn 200 calls 4-at-a-time x50 waves (allowed until MaxToolCalls after turn); no CPU quota (honestly No resource limits).

## 22. Tests

166 *_test.go (runtime 35,tui 22,providers 19,shell 12,session 12). Density 2.9-4.2/test. nvidia zero-assert false positive (helpers assert). Only smoke healSession(nil) intentional. Strong: filesystem/sandbox_test, policy_test, retry_test, context_test, turn_retry/idempotency/tool_limits/trace/workspace/redact_boundary/repair/scrub/growth/durability/coalesce, HIT/VIEW-ground geometry, shell real exec+cancel+gate, git real init/add, WSL integration 2s, TOCTOU/strict/sensitive. Missing: no live-model E2E (all fakes panic on fixture error), no 60-iter soak, no 429 storm across turns, no crash-during-Sync->Rename injection, no mid-SSE [DONE]/partial-JSON, no Windows long-path, no concurrent out-of-order, no 5-day soak, no injection test, no -count=10 stress (CI no -count/-race/-shuffle; zero t.Parallel; Chdir-global + Pipe leaks + Sleep 30-600ms + After 5s/15s flake; Windows skips perm bits silently). Makefile gate only cmd>=30% — global 25k LOC no gate. Doctor nil-guards pass degraded silently.

## 23. Build/release

Makefile -trimpath -ldflags -s -w -X ...Version matches dual cmd/main.Version (easy stamp one -> dev). test no -count/-race/-shuffle; lint no config (non-repro); release 6 artifacts + python checksum fallback; clean rm -rf breaks PS. go.mod go 1.26.4 future-pinned (README 1.22+ drift); direct deps minimal (yaml.v3,cobra,bubbles/bubbletea/lipgloss,uuid,termenv), no vendoring, no CUE lib (out-of-band preserves single-binary). go.sum + verify good. flake nixpkgs:nixos-unstable floating + unused goreleaser. release.yml vs Makefile vs installers names match (CGO_ENABLED=0), smoke --version + count==6 + sha256sum -c good; installers injection rejected, mktemp -d 700+trap, atomic cp->chmod->mv, never touch ~/.forcefield except warn, uninstall refuses inside it never deletes config/sessions. Gaps: no cosign/SLSA/SBOM, contents:write broad, local make release dev-stamped checksums, checksum skipped with warning if missing, PATH appends weak idempotency. CI 3-OS fail-fast:false 20m, gofmt (excl main.go), vet, test + -race, build (double suite no cache), CUE latest via curl|tar jq floating no pin/hash, coverage dup gate, CodeQL security-extended+autobuild, gitleaks v2.3.9. .gitleaksignore 9 fakes pinned to dc4d2e1 — rots on rebase. .gitignore correct; bin/ff.exe untracked stale on disk. CUE vs Go: Permission allow/deny/ask/"" matches; timeout 0..300 matches; max_bytes/lines >0 CUE rejects 0 Go allows 0=unset intentional; workspace/sandbox enums match; #Provider 15 vs IsKnownType+alias (alias no fixture); tools.cue 13 sync knownTools but defaultConfigTemplate 11 (missing load_skill,update_task_state default ask via manager not allow as tools.cue documents); providers.cue keys opencodezen/go vs hyphenated IDs confusing; invalid-limit -5 CUE rejects runtime ignores intentional, but per-agent negatives pass Go entirely.

## 24. Docs

Mirrors pkgs; Sandbox honestly WSL not FS-confined + Command text not filtered + No resource limits; Config secrets never persisted; Session atomic/corrupt/List correct; Tools limits exact; Command no-BubbleTea enforced. Drifts MUST-FIX: D1 CLI ff init/tools/sessions, slash /resume — neither exists (only run/chat/doctor/memory, --resume flag + /sessions picker); D2 model/provider in-memory only vs SaveConfig persists; D3 sessions .forcefield/sessions, memory ~/.forcefield/memory.md vs cwd-relative per-project + project-scoped memory; D4 doctor LM Studio/NVIDIA vs generic probes. Plus go.mod 1.26.4 vs README 1.22+; Runtime Events 6 vs 8 actual (+Progress/Cancelled/Blocked/Denied); Index omits task/permissions; Providers One inference per provider true via runMu not provider gate (misleading standalone); Config.md:215 omits ""=ask; no nohup/tmux 5-day, billing/quota, wsl escape quickstart, window guidance, cwd warning.

## 25. Contracts

runtime<->provider explicit+enforced (single-method, NewFromConfig rejects nil, Spec never logged, Validate before construct). runtime<->tools<->scheduler<->permissions mostly explicit (Filtered rejects unknown/duplicate, Context TUI-independent, askMu, session-allow never persisted). session<->persistence<->memory atomic but Messages nil vs [] not normalized, AddMessage nil *Session panics (only healSession(nil) guards). Traps: scheduler MaxConcurrency<=0->1/MaxRetries<0->0/BaseBackoff<=0->200ms silently coerces invalid instead of erroring; Limits.WithDefaults <=0->default hides programmatic negatives; ParseWorkspaceMode ""->permissive, nil Tools->empty, Definition nil Tools/Skills=all vs explicit empty=none (critical skills: [] pinned only comments+test); TUI Update:419 (247)+handleKey:978 (130)+refreshTranscript:1435 (163) duplicates runtime turn-state (stranded repair both session and TUI — two truths for paired); MustRegister/MustGet/panic startup duplicates violates never panic (defensible undocumented exception).

## 26. Failure matrix

429 transient: yes/yes-3x-jitter-60s/yes/yes/partial (sustained exhausts; RESOURCE_EXHAUSTED miss). Quota: yes/no-correct/yes/yes/halts. 500/502/503/504/529 pre-200: yes/YES-fixed/yes/yes/yes. Timeout 120s headers: yes/YES-pre-200-fixed/yes/yes/no if TTFB>120s Ollama. Mid-SSE disconnect: yes/no-by-design/partial/yes/no half lost aborts. Malformed SSE/JSON: yes/no-correct/yes/yes/yes. Oversize SSE>1MiB: yes/no Unknown-not-Protocol/yes/yes/minor. Tool crash: yes/yes-next-turn/yes/yes/yes. Hang 30s: yes Timeout+pgroup/yes/yes/yes/partial Windows leak. Huge output: yes 2MiB+6000c+40-line/YES-fixed/yes/yes/yes (LLM may miss — tradeoff). Huge input 20k: yes/yes/yes/-/yes. Infinite loop: yes 60/300/5+loop-3-dirty/yes/yes/yes by default; limits<=0 unbounded. Truncated length+tool calls: yes/no-EXECUTES-truncated/NO/no/no BUG. Crash/panic: no recover/no/last-tool-saved-1-lost/no log/no. Terminal close: no SIGHUP/no/last Save/-/no need tmux. Reboot: files survive/manual --resume/atomic/doctor/no auto-restart. Corruption: yes/yes-isolates/never-delete/doctor/yes. Disk full: yes/no-diverges/mem!=disk/yes/no. Network pre-200: yes/YES-fixed/yes/yes/yes. Local model down: yes hint/no fallback/yes/yes/manual /provider. Malicious repo injection: no delimiter/no/poisoned/no/no. Concurrent 4x ask: yes/YES-fixed-askMu/yes/yes/yes. Shell /mnt wsl: lexical-only/no/no/no/no. kill -9 mid-Save: atomic yes old-or-new/yes/-/yes 1-lost. Concurrent same-session: no lock/no last-wins lost/no/no.

## 27. Adversarial agent

Repeats forever -> loop-3 (dirty)+60/300/5 -> Blocked. Enormous files -> 5MiB refuse+6000c+window. Huge shell -> 2MiB+300s+pgroup kill. Retries failed -> consecutive-5 -> Blocked; exec-errors retryable only if Retryable (shell false correct). Reads secrets -> auto-allow + lexical bypass via rename/symlink -> provider+session (no cage default). Leaves workspace -> native allowed; strict/wsl FS tools blocked, shell allowed (/mnt lexical only). Ignores results -> loop catches exact repeats, not semantic ignoring (alternating evades, bounded caps). Malicious repo -> fenced-unescaped + memory unfenced -> hijack plausible. Invalid args -> ValidateArgs after permission (extra ignored, wrong-type defaults some paths) UI truncated. Contradictory parallel -> 4-concurrency+emitMu+idempotent IDs safe. Malformed -> protocolError fail-fast. Never concludes -> iteration cap -> Blocked. Forcefield imposes RESOURCE boundaries well; SAFETY boundaries are prompts+honesty, not isolation — correct local-first only if user understands native=full user privileges.

## 28. Regression e10e62a -> Commit 100

FF-SEC-001 WSL /mnt: STILL PRESENT. FF-SEC-002 native auto-allow: STILL PRESENT. FF-CRIT-003 unbounded context: PARTIALLY (window 100+tokens + 1000 cap + FinishLength zero-call; system bypasses, Summarize false, task unbounded, in-mem unbounded). FF-SEC-003 Always: PARTIALLY (session-scoped, still per-name). FF-SEC-005 OOM: RESOLVED (2MiB). FF-SEC-006 injection: PARTIALLY (fence no escape, memory unfenced). FF-SEC-007 session leak: PARTIALLY (scrub boundaries, nested-env leak, plaintext 0600). FF-SEC-009 concurrent ask: RESOLVED (askMu). FF-REL-001 5xx/timeout/mid-stream: PARTIALLY (transport RESOLVED, stream STILL PRESENT). FF-REL-002 session growth: PARTIALLY (1000 silent-drop, no rotation/marker/GC/delete). FF-TUI-001 resume drops tools: RESOLVED (uncommitted model.go:381-402). FF-SEC-004 bash -c: PARTIALLY (recursed, heuristic bypassable). FF-SEC-010 TOCTOU: PARTIALLY (O_NOFOLLOW Unix; Windows no-op; secret_scan none). FF-SEC-011 timeout: RESOLVED (>300 ArgumentError+Clamp+scheduler). FF-SEC-012 Windows kill: PARTIALLY (taskkill+WaitDelay+drain; grandchildren survive). FF-CTX-002 FinishLength->Done: PARTIALLY + REGRESSED subset (zero-call ->Blocked; with-calls EXECUTES truncated). FF-PERM-001 truncation: STILL PRESENT by design. FF-SESS-001 coalesce: PARTIALLY (<10s+ID-dedup; time not Turn.ID). FF-OBS-001 no log: PARTIALLY (opt-in JSONL 4MB redacted crash-kept; reasoning not saved; off default). FF-CFG-001/DOC/HYG: STILL PRESENT (Go drift, Runtime/Config/CLI drifts, tree dirty new files).

NEW since last audit: FinishLength-with-calls exec, Gemini reasoning/IDs/Stop, Ollama bare-Done/EOF/request-reuse, SSE-oversize Unknown, ScrubMap nested-env leak, write_file unbounded input, search within case-sensitivity, Strict FilesystemConfined overclaim, shell_job missing WSL check, env key gap, CJK underestimate, in-mem unbounded limits<=0, consumer-abandon runMu deadlock, TUI-owned TurnState, UpdatedAt churn, memory unbounded/stale/dead-global, no session delete, trace FD/file-count, stdin park, discovery leak, ff chat --resume trap, stdout pollution, /sessions conflation, /status bytes-as-context, D2 persist lie.

## 29. Quality

Idiomatic Go, small pkgs, explicit returns, fmt.Errorf %w, no panics expected (except MustRegister startup undocumented), zero TODO in Go, good naming, no globals except redact.secrets. Duplicated 3x atomic write (config vs session+replaceFile vs memory), 2x openNoFollow, Nx truncation wording (shared cutting per-tool wording by design). Oversized justified: run() 123, newRuntime() 117, SetAgent() 104, runOneWithManager() 184, doctorProvider() 118, TUI Update() 247/handleKey() 130/refreshTranscript() 163, StreamChat 105-155 each — splitting adds abstraction without reducing coupling. Confusing: scheduler.managerMu guards manager+asker; 4 agent reps (prompt/Config/Definition/Session string); TUI model holds session+runtime+pickers+transcript. Dead: dual Version vars, EventToolCall/Result+Status aliases, runGit unbounded helper, flake goreleaser, permOptionGap/layout consts. .gitleaksignore commit-pinned rots on rebase.

## 30. Risk model (selected)

CRITICAL: FF-100-C01 native zero confinement + auto-allow reads -> zero-click .env/.ssh -> provider -> session replay. config.go:236-242,259,271 + builtin.go:96-102. High likelihood, host-wide. STILL PRESENT. | FF-100-C02 WSL shell escapes via /mnt/quoting//proc/self/root; shell_job lacks lexical check. sandbox.go:227-232,wsl_windows.go:15-44,shell.go:329,727-760. High if wsl as sandbox. STILL PRESENT. | FF-100-C03 FinishLength with tool calls executes truncated args. runtime.go:1827-1844 + finish_length_test.go:49 pins. Medium likelihood, arbitrary exec partial JSON. CONFIRMED BUG new.

HIGH: H01 system bypasses budget + unbounded task mem -> overshoot. context.go:271,215 + task/state.go:279-289. | H02 Always-per-name shell in session authorizes any future command. scheduler.go:58-64,409-423,537-542. PARTIALLY. | H03 injection via unescaped fence + unfenced memory. scrub.go:12-14 no escape; agent.go:61-72 plain. PARTIALLY. | H04 nested shell.env secrets raw via shallow ScrubMap. redact.go:148-161 + shell.go:977-997. CONFIRMED narrow vuln. | H05 write_file unbounded input disk fill. write_file.go:51-119. | H06 mid-stream fatal by design; slow-TTFB>120s Ollama fatal; no resume. retry.go:163-168, ollama.go:132. PARTIALLY. | H07 in-mem messages unbounded; limits<=0 unlimited. runtime.go:32-33,1846,1870. | H08 concurrent same-session last-wins no lock; Session no mutex. manager.go:62-137. | H09 Strict FilesystemConfined:true overclaim for shell; UI overstates. native.go:72-84 vs sandbox.go:242-245. | H10 dirty tree uncommitted loop/cancel/resume; CI irreproducible.

MEDIUM/LOW: per-provider quirks (Gemini/Ollama/SSE), search within case, env key gap, CJK, abandon runMu deadlock, TUI-owned TurnState, 10s coalesce, UpdatedAt churn, memory unbounded/stale/dead-global, no session delete, trace FD/count, stdin park, discovery leak, Windows grandchild, job WSL parity, TOCTOU Windows + secret_scan none, chat --resume trap, stdout pollution, /sessions conflation, /status bytes, uncapped transcript errors, D1-D4 lies, dev version, no verbose/tokens. Correctly implemented: runtime owns loop; providers one-turn never exec tools; TUI presentation-only (except repair); atomic tmp+Sync+Rename x5 +0600/0700 +never-delete-corrupt+ListCorrupt; validID; Turn Recover never re-run +Repair idempotent paired +heal; runMu+snapshot; dup-ID idempotency; askMu+emitMu+caps; 429 bounded+jitter+Retry-After+quota +5xx/timeout pre-200 +header-only +IsTransient cancellable; shell 2MiB+300s+Stdin nil+pgroup/taskkill+WaitDelay+drain+bash-c recursion+sanitize; FS ResolveWithinWorkspace+EvalLinks+re-validate+O_NOFOLLOW Unix strict/WSL; secrets never YAML/env-inherit +header keys+hashed discovery+scrub +doctor redaction; per-turn window +FinishLength zero-call Blocked +1000 cap+tool-render dirty; incremental markdown cache+ASCII guard+live tools+following-pause+measured header/pickers; doctor probes+key-source+sandbox honesty; 3-OS -race +CodeQL+gitleaks; zero TODO.

## 31. Scores (0-100)

Architecture 78; Runtime 80; Agent Loop 82; Long-Horizon 48; Context 55; Provider Reliability 72; Tool Safety 62; Shell Safety 68; Filesystem/Sandboxing 45; Security 52; Sessions 74; Memory 42; Persistence 78; Concurrency 80; Cancellation 85; CLI/TUI 78; Ease of Use 62; Cross-Platform 72; Configuration 70; Error Handling 76; Observability 58; Resource Mgmt 64; Testing 70; Release Eng 68; Documentation 60; Maintainability 76.

Overall Engineering Quality 68. Overall Security Posture 52. Overall Reliability 64. Overall User Experience 70. Overall Long-Horizon Readiness 48. Overall Production Readiness 55.

## 32. Verdict

A. Hardening (continue): GO — sound, no rewrite, incremental bounds work.
B. Public RC (with warnings): CONDITIONAL GO — ship only with D1-D4/cwd/native=full-user/WSL!=cage fixed, dev-version + stdout pollution fixed; dotfiles ask or workspace-relative reads advised.
C. Long-horizon unattended (hours-days, no human): NO-GO — system bloat + mid-stream fatal + slow-TTFB + nested leak + FinishLength-with-calls + native defaults + no restart/log/rotation make 24h+ unsafe.
D. Production (untrusted repos, adversarial models): NO-GO — zero-confinement default + shell escape + per-name Always + unescaped fence + shallow scrub + last-wins sessions are not a production boundary.

Top 10 risks: C01 native auto-allow, C02 WSL escape, C03 FinishLength-with-calls, H01 system-bloat, H02 Always-per-name, H03 unescaped fence+unfenced memory, H04 nested-env leak, H06 mid-stream/slow-TTFB, H07 in-mem unbounded limits<=0, H10 dirty-tree.
Top 10 bugs: FinishLength-with-calls; Gemini reasoning dropped; Gemini synthetic IDs; Gemini tool-Stop; Ollama bare-Done; Ollama EOF->UnexpectedEOF; Ollama ListModels reuse; SSE-oversize Unknown; ScrubMap nested passthrough; Strict FilesystemConfined overclaim (+chat --resume trap + stdout pollution UX).
Top 10 vulns: native zero cage; WSL escape (+job parity); Always-per-name; unescaped fence; unfenced memory; nested-env persist; write_file unbounded fill; TOCTOU Windows + secret_scan none; search within case; last-wins sessions (+update_task_state passed trust).
Top 10 UX: D1-D4 lies; cwd-resume/trace trap; silent-busy; silent copy; Tab only command-name; footer omits shortcuts; /sessions conflation; /status bytes-as-context; uncapped transcript errors; dev version + no update + stdout pollution.
Top 10 long-horizon: system-bloat; in-mem msgs; task mem; session silent-drop/no rotation/GC/delete + O(n) List; trace file-count/FD; memory unbounded/stale; mid-stream fatal; slow-TTFB; no SIGHUP/restart/log; disk-full divergence + last-wins.
Top 10 arch: god-object Runtime; TUI-owned TurnState; shared Tool instances; single Messages slice; single session file no lock; managerMu overload; 4 agent reps; dual repair truth; events cap1+runMu abandon deadlock; global redact.secrets.
Most dangerous unknowns: real-model 60-turn pressure (no soak); sustained-429/billing over days; Windows grandchild accumulation; concurrent-session loss rate; fence escape success rate (no red-team); limits<=0 OOM threshold; CJK overshoot magnitude.
Most important missing tests: 60-iter soak + growth (msgs/session/disk/RSS); 5xx/timeout/mid-stream chaos incl [DONE]/partial-JSON; FinishLength-with-calls completeness; nested-env scrub; write_file quota; cross-agent Always revocation; concurrent same-session; kill -9 mid-Sync/Rename + resume; Windows job-object leakage; fence escape (</tool_result>); -race -count=10 -shuffle stress.
What Commit 100 accomplished: committed P0-P1.14 real — header-only timeouts, 5xx/timeout pre-200 jitter/Retry-After/cancel, 2MiB shell +300s ceiling +Clamp, askMu, session-scoped Always, 100-msg window +tokens +FinishLength zero-call Blocked +1000 cap, <tool_result> fence + boundary scrub + redact central +AddSecret, bash -c recursion, O_NOFOLLOW+re-validate Unix, atomic writes +Turn envelope +heal +ListCorrupt, opt-in redacted JSONL trace, honest Sandbox/config comments, 3-OS -race + extensive tests. Dirty adds runMu, EventCancelled, RunControl wait, loop-3, tool-render resume, loading wave. vet/build/test(-race) pass. Serious hardening, not theater.
What still prevents prod-grade: native/permissive + auto-allow (zero-click exfil); shell text unconfined every mode (lexical WSL not cage, job lacks it, Strict overclaims); per-name Always; unescaped fence + unfenced memory + shallow nested scrub; FinishLength-with-calls exec; system bypassing budget + unbounded task/memory/file-count + silent-drop + no rotation/GC/delete/log/restart; mid-stream + slow-TTFB fatal; last-wins sessions; Windows/WSL pgroup gaps; D1-D4 + cwd traps + stdout pollution; dirty-tree irreproducibility. Fix those, add soak+chaos+red-team, pin CUE/signing/version, then CONDITIONAL GO long-horizon under strict + tmux + monitored billing + manual resume — not yet unattended 5-day or adversarial production.

*Audit only. No source modified. Dirty files (loop_detector.go, run_control.go, loading.go, new.go, modified runtime.go/event.go/jobs.go/model.go/messages.go/commands.go/styles.go/go.mod) mean HEAD alone does not reproduce this report — commit or stash before hardening.*
